# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-march=native -O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"

USE="${USE} gnuefi systemd mmx sse sse2 zsh-completion lua luajit 256-color blink xft unicode3 seconday-wheel dbus X tiff curl cairo jpeg png xinerama opengl pm-utils alsa gpg sqlite jemalloc offensive ruby python hscolour binary logrotate initramfs truetype corefonts amr amrenc cpudetection mp3 ogg flac openal vorbis lame x264 x265 xcb ipc mpd network networkmanager highlight json bluetooth pixbuf input_devices_libinput filecaps vim-syntax fading-colors ffmpeg pulseaudio fontconfig acl system-llvm rls i3wm lm_sensors cjk extra graphics xetex btrfs f2fs fat hfs ntfs xfs libnotify libmpv sdl hwaccel"

MAKEOPTS="-j7"

INPUT_DEVICES="libinput"

VIDEO_CARDS="intel i965"

ACCEPT_KEYWORDS="~amd64"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/usr/portage"
DISTDIR="/usr/portage/distfiles"
PKGDIR="/usr/portage/packages"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

RUBY_TARGETS="ruby25"

GENTOO_MIRRORS="http://mirror.csclub.uwaterloo.ca/gentoo-distfiles/ ftp://ftp.gtlib.gatech.edu/pub/gentoo ftp://ftp.ussg.iu.edu/pub/linux/gentoo"
